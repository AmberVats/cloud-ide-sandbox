{
  "name": "cloud-ide-sandbox",
  "version": "1.0.0",
  "description": "⚡ Browser-based Collaborative Cloud IDE & Sandboxed Code Execution Engine with Docker cgroups, Monaco Editor, Xterm.js & WebSockets.",
  "keywords": [
    "cloud-ide",
    "code-sandbox",
    "docker",
    "cgroups",
    "monaco-editor",
    "xtermjs",
    "websockets",
    "react",
    "typescript"
  ],
  "author": "Amber Vats",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "install:all": "npm install && npm install --workspace=client && npm install --workspace=server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
